kind: Deployment
apiVersion: apps/v1
metadata:
  name: mydep
spec:
  replicas: 3
  selector:
    matchLabels:
      name: deployment
  template:
    metadata:
      name: testpod1
      labels:
        name: deployment
    spec:
      containers:
        - name: c00
          image: nginx
          command: ["/bin/bash", "-c", "while true; do echo hi I am learning k8s; sleep 5; done"]

